# Zabbix Agent2 交叉编译产物清单

> 本文档与同目录下的 `zabbix_agent2`（aarch64 二进制）及 `zabbix_agent2-aarch64-kylin.tar.gz`（部署包）配套，
> 便于在文档视图中直接查看产物状态。完整编译方案见同目录 `zabbix-agent2-arm64-crosscompile.md`。

## 1. 产物文件

| 文件 | 说明 |
|---|---|
| `zabbix_agent2` | aarch64 二进制本体（18 MB，非文档类，文档视图不预览，可直接下载/`scp`） |
| `zabbix_agent2-aarch64-kylin.tar.gz` | **开箱即用部署包**（8.4 MB，含 bin+deploy+前缀对齐配置+目录） |
| `zabbix-agent2-arm64-crosscompile.md` | 完整交叉编译 SOP |
| `产物清单.md` | 本文件 |

### 1.1 二进制校验

| 项 | 值 |
|---|---|
| 大小 | 18,104,896 字节 |
| 类型 | ELF 64-bit LSB executable, **ARM aarch64**, dynamically linked |
| 解释器 | `/lib/ld-linux-aarch64.so.1` |
| SHA256 | `c8dc3bb80f69d2fbce1d23b421fa68effbd2fc76c687db62f488850eeb328adf` |
| 是否 strip | 否（含 debug_info） |

## 2. Kylin V10（glibc 2.28）兼容性校验

| 检查项 | 结果 |
|---|---|
| 架构 | ✅ ARM aarch64 |
| 最高 GLIBC 需求 | ✅ **GLIBC_2.28** |
| NEEDED 库 | ✅ libresolv / libdl / libpthread / libgcc_s / libc / ld |
| Docker 插件 | ✅ 已编译进二进制（90 处引用） |
| Oracle 插件 | ✅ 已排除（0 处引用） |

## 3. 运行时验证（qemu-aarch64 模拟执行）

```bash
qemu-aarch64-static -L /usr/aarch64-linux-gnu /workspace/zabbix_agent2 --version
```
输出（退出码 0）：
```
zabbix_agent2 (Zabbix) 5.2.1
Revision {ZABBIX_REVISION} 9 November 2020, compilation time: Jul 28 2026 22:14:51
...
```

## 4. 部署包内容（zabbix_agent2-aarch64-kylin.tar.gz）

解压后即为完整 prefix 目录，路径已对齐 `/home/zabbix/zabbix_agent2`：

```
zabbix_agent2/
├── bin/zabbix_agent2                 # aarch64 二进制
├── etc/zabbix_agent2.conf            # 已对齐 prefix 的配置（LogFile/PidFile/ControlSocket/Include）
├── etc/zabbix_agent2.d/              # Include 子配置目录
├── deploy/
│   ├── zabbix-agent2.service         # systemd 单元
│   └── zabbix_agent2.sh              # start/stop/restart/status 脚本
├── logs/  run/  share/  plugins/     # 运行时目录
```

> 二进制已内置默认配置路径 `main.confDefault=/home/zabbix/zabbix_agent2/etc/zabbix_agent2.conf`，
> 因此解压后即使不加 `-c` 也能找到配置。

## 5. 部署到 Kylin V10（arm64）—— 两种用法

### 用法 A：直接用部署包（推荐）

```bash
# 1) 传包并解压到 /
scp zabbix_agent2-aarch64-kylin.tar.gz kylin:/tmp/
ssh kylin "cd / && tar -xzf /tmp/zabbix_agent2-aarch64-kylin.tar.gz"

# 2) 建用户 + 赋权 + 加入 docker 组
ssh kylin "useradd -M -s /sbin/nologin zabbix 2>/dev/null; usermod -aG docker zabbix; \
  chown -R zabbix:zabbix /home/zabbix/zabbix_agent2; \
  chmod +x /home/zabbix/zabbix_agent2/bin/zabbix_agent2 /home/zabbix/zabbix_agent2/deploy/zabbix_agent2.sh"

# 3) 启动（二选一）
#  systemd:
ssh kylin "cp /home/zabbix/zabbix_agent2/deploy/zabbix-agent2.service /usr/lib/systemd/system/ && \
  systemctl daemon-reload && systemctl enable --now zabbix-agent2"
#  或脚本:
ssh kylin "/home/zabbix/zabbix_agent2/deploy/zabbix_agent2.sh start"
```

### 用法 B：只传二进制（若 Kylin 上已有配置/启动方式）

```bash
scp /workspace/zabbix_agent2 kylin:/home/zabbix/zabbix_agent2/bin/
# 再按上面的启动步骤即可
```

### 部署前必改项（配置里的占位符）

编辑 `etc/zabbix_agent2.conf` 把以下占位改成真实值（当前为 127.0.0.1 / Zabbix server 占位）：

```ini
Server=<ZABBIX_SERVER_IP>
ServerActive=<ZABBIX_SERVER_IP>
Hostname=<HOSTNAME>          # 必须与 Zabbix 前端配置一致
```

### 验证 docker 插件真实可用

```bash
zabbix_get -s 127.0.0.1 -k docker.info        # 期望返回 Docker 守护进程信息
zabbix_get -s 127.0.0.1 -k docker.containers   # 容器列表
```

## 6. 改端口（被动监听，默认 10050）

编辑 `etc/zabbix_agent2.conf`：
```ini
ListenPort=10055          # 范围 1024:32767，无 CLI 参数，只能改配置文件
ListenIP=0.0.0.0          # 可选，绑定网卡
```
改完重启 agent 生效。主动模式连 Server 的端口在 `ServerActive=<IP>:<port>`（Server 端默认 10051），与 agent 自身监听端口无关。
