# 产物清单 — Zabbix Agent2 5.2.6（aarch64 / 麒麟 V10 / glibc 2.28 / 含 MongoDB）

> 本文档对应 **5.2.6** 构建产物，区别于 5.2.1 的 `产物清单.md`。
> 构建方式：方案 B（主机交叉编译，prefix 不变 = `/home/zabbix/zabbix_agent2`，CI 目录 `/tmp/zbx526`，制品命名加 `_5.2.6`）。

---

## 1. 工作区产物（/workspace）

| 文件 | 大小 | 版本 | 说明 |
|---|---|---|---|
| `zabbix_agent2` | 18,104,896 B | 5.2.1 | 原产物，**未覆盖** |
| `zabbix_agent2_5.2.6` | 21,041,792 B | **5.2.6** | 新二进制（本任务交付） |
| `zabbix_agent2-aarch64-kylin.tar.gz` | 8,769,814 B | 5.2.1 | 原部署包，**未覆盖** |
| `zabbix_agent2-aarch64-kylin-5.2.6.tar.gz` | 10,101,251 B | **5.2.6** | 新部署包（内部顶层 `zabbix_agent2/`） |
| `zabbix-agent2-arm64-crosscompile.md` | — | 5.2.1 | 原 SOP，**未覆盖** |
| `zabbix-agent2-arm64-crosscompile-5.2.6.md` | — | 5.2.6 | 新 SOP（本任务交付） |
| `产物清单.md` | — | 5.2.1 | 原清单，**未覆盖** |
| `产物清单-5.2.6.md` | — | 5.2.6 | 本文档 |

---

## 2. 二进制验证结果

| 检查项 | 命令/方法 | 结果 |
|---|---|---|
| 架构 | `file` | ELF 64-bit LSB executable, **ARM aarch64** |
| 版本 | `qemu-aarch64-static -L /usr/aarch64-linux-gnu ... --version` | **`zabbix_agent2 (Zabbix) 5.2.6`**，exit 0 |
| glibc 需求 | `objdump -T | grep GLIBC_` | **GLIBC_2.17 + GLIBC_2.28** → ≤2.28，麒麟兼容 ✅ |
| 动态依赖 | `readelf -d | grep NEEDED` | libdl / libresolv / libpthread / libc / ld-linux（无 libclntsh/libpcre 等硬依赖） |
| MongoDB 插件 | `strings | grep plugins/mongodb` | **399** 处，`zabbix.com/plugins/mongodb.(*Plugin).Configure/Validate`、`(*MongoConn).Ping` 等 |
| 运行时加载 | qemu + 最小 conf 启动 | `using plugin 'Mongo' providing following interfaces: exporter, runner, configurator` ✅ |
| 默认配置路径 | ldflags `main.confDefault` | `/home/zabbix/zabbix_agent2/etc/zabbix_agent2.conf`（prefix 不变） |

### qemu 版本输出（节选）
```
zabbix_agent2 (Zabbix) 5.2.6
Revision {ZABBIX_REVISION} 29 March 2021, compilation time: Jul 30 2026 13:24:04
```

### 运行时插件加载（节选，证明 MongoDB 可用）
```
Starting Zabbix Agent 2 (5.2.6)
using plugin 'Docker' providing following interfaces: exporter, configurator
using plugin 'Mongo'  providing following interfaces: exporter, runner, configurator
using plugin 'Mysql'  providing following interfaces: exporter, runner, configurator
using plugin 'Oracle' providing following interfaces: exporter, runner, configurator
using plugin 'Postgres' providing following interfaces: exporter, runner, configurator
```

---

## 3. 已编译插件列表（5.2.6 比 5.2.1 更多）

Agent / Ceph / Cpu / Docker / File / **Kernel** / Log / Memcached / Memory / Modbus / **Mongo** / MQTT / Mysql / NetIf / **Oracle** / Postgres / Proc / ProcExporter / Redis / Smart / System / Systemd / SystemRun / VFS / VM / Web / Zabbix

> 说明：Oracle/MySQL/Postgres 等采用纯 Go 驱动，NEEDED 列表无对应原生客户端库（libclntsh/libmysqlclient/libpq），不引入 2.28 之外的硬依赖。

---

## 4. 部署包内部结构

```
zabbix_agent2/
├── bin/zabbix_agent2
├── etc/zabbix_agent2.conf              # 已对齐 /home/zabbix/zabbix_agent2 路径
├── etc/zabbix_agent2.d/                # 用户插件配置目录
├── logs/
├── run/
├── plugins/
├── share/
└── deploy/
    ├── zabbix-agent2.service           # systemd（zabbix 用户）
    └── zabbix_agent2.sh                # start/stop/restart/status
```

---

## 5. 与 5.2.1 的衔接

- **版本兼容**：server 锁定 5.2.1；agent 5.2.6 同 minor 线、最小偏移，官方支持（agent ≤ server）。
- **MongoDB**：5.2.1 无内置 MongoDB 插件；5.2.6 已内置，无需额外编译或外部插件包。
- **RabbitMQ**：两版本均无原生插件，统一用 HTTP 模板监控。
- **端口**：`etc/zabbix_agent2.conf` 中 `ListenPort`（默认 10050）；或 `zabbix_agent2 -c <conf> -p <port>` 临时指定。

---

## 6. 快速部署（目标机 麒麟）

```bash
# 1) 拷贝 zabbix_agent2-aarch64-kylin-5.2.6.tar.gz 到目标机 /home/zabbix/
# 2) 解压
cd /home/zabbix && tar xzf zabbix_agent2-aarch64-kylin-5.2.6.tar.gz
# 3) MongoDB 监控：在 etc/zabbix_agent2.d/ 增加 mongodb 配置（URI/账号）
# 4) 启动
/home/zabbix/zabbix_agent2/deploy/zabbix_agent2.sh start
# 或 systemd
cp /home/zabbix/zabbix_agent2/deploy/zabbix-agent2.service /usr/lib/systemd/system/
systemctl enable --now zabbix-agent2
```
